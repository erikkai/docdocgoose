[project]
name = "docdocgoose"
version = "1.0.0"
description = "DocDocGoose CLI"
requires-python = ">=3.10"
dependencies = [
    "click>=8.3.1",
]

[project.scripts]
ddg = "docdocgoose.ddg:cli"

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"